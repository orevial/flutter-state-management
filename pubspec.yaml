---
name: state_management
description: Sample app to demonstrate differences between various state management
  librairies
publish_to: none
version: 1.0.0+1
environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  intl: ^0.17.0
  bloc: ^7.2.0
  flutter_bloc: ^7.3.3
  provider: ^6.0.1
  mobx: ^2.0.5
  flutter_mobx: ^2.0.2
  flutter_riverpod: ^1.0.0
  redux: ^5.0.0
  flutter_redux: ^0.8.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
  build_runner: ^1.12.2
  mobx_codegen: ^2.0.0

flutter:
  uses-material-design: true
